# THROWAWAY input for scripts/probe_oai.py. Delete with it.
#
# Round 1 (run 35070090443) settled the claim: andmed.eesti.ee serves OAI-PMH
# at /oai as "Estonian OpenData", metadataPrefix=dcat_ap is the accepted value
# (oai_dcat and dcatap are rejected), /oai/hvd and /oai/dga exist as separate
# base URLs accepting only ListRecords, and the status page is live. Two
# protocol defects: ListMetadataFormats answers badArgument when called bare,
# which OAI-PMH does not allow, and ListSets answers noSetHierarchy, so
# neither the format nor the subsets are discoverable through the protocol.
#
# Round 2 (run 35070340447) read the records: 8 053 identifiers in a single
# unpaginated response, and seven of our records in the HVD subset, none of
# them carrying an endpointDescription.
#
# Round 3 prints per DCAT node rather than per record. Round 2 flattened each
# record whole, which lost which endpointURL belonged to which service and
# which accessURL to which distribution — and that distinction is the whole
# question. 8 800 lines of output was also more than anyone needs.

# The full catalogue, filtered to our own records, one block per Dataset,
# DataService and Distribution. This is what re-verifies the letter: how many
# of our catalogue entries carry a real endpointURL, how many carry an
# endpointDescription, and whether the Ilmateenistus XML feeds appear as
# distributions anywhere.
harvest:https://andmed.eesti.ee/oai?verb=ListRecords&metadataPrefix=dcat_ap

# Round 4 answered the subsets: 7 of our records are in hvd, none in dga
# (which holds one record in total), and not one of the eight named service
# records carries an endpointDescription. Both are dropped from round 5 —
# re-downloading them would tell us nothing new.
#
# What round 4 left open: the full harvest returned 36 Keskkonnaagentuur
# datasets against the 24 the web page lists, and EstModel — the one service
# with a real OpenAPI description — was in neither the dataset list nor the
# service list. WATCH in the script now scans every one of the 8 053 records
# for it, whoever the publisher is, which settles whether it is absent from
# the export or merely attributed to someone else.
