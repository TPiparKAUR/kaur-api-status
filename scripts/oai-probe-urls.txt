# THROWAWAY input for scripts/probe_oai.py. Delete with it.
#
# Round 1 (2026-09-16, run 35070090443) established that andmed.eesti.ee does
# serve OAI-PMH at /oai — repositoryName "Estonian OpenData", protocol 2.0,
# adminEmail opendatasupport@ria.ee — that metadataPrefix=dcat_ap is accepted
# and oai_dcat / dcatap are not, and that /oai/hvd and /oai/dga exist but
# answer only the ListRecords verb. Two defects came out of the same round:
# ListMetadataFormats answers badArgument without an identifier, which the
# protocol does not allow, and ListSets answers noSetHierarchy, so neither the
# format nor the subsets are discoverable through the protocol itself.
#
# Round 2 reads the records. ListRecords returned more than the 8 MiB the
# probe was reading, so nothing could be parsed; the cap is now 256 MiB with
# gzip requested.

# The whole catalogue, harvested and filtered down to our own records. This is
# what re-verifies the letter's numbers: every DataService record's
# dcat:endpointURL and dcat:endpointDescription, and every Dataset's
# distributions, read from the publisher's own metadata rather than from crawl
# notes.
harvest:https://andmed.eesti.ee/oai?verb=ListRecords&metadataPrefix=dcat_ap

# The High-Value Datasets subset, which the catalogue's web pages show our
# meteorological and monitoring datasets tagged into. Its own base URL, since
# ListSets says this repository has no set hierarchy.
harvest:https://andmed.eesti.ee/oai/hvd?verb=ListRecords&metadataPrefix=dcat_ap

# ListIdentifiers is the cheap way to get a total; worth having next to the
# harvest count as a cross-check.
https://andmed.eesti.ee/oai?verb=ListIdentifiers&metadataPrefix=dcat_ap
