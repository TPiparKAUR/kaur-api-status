# One-off input for scripts/probe_catalogue.py (round 5). Delete both
# once the endpoints they reveal are in config/endpoints.toml.
#
# Sources the project owner named directly. Two are new: the API_liides
# directory of interface documentation, and the forecast XML page (the
# observation XML page was already read in round 4, and is re-read here
# in full because its own-host links were being dropped).
#
# 'all:' keeps a page's own-host links: a directory index and a feed's
# documentation page both point at themselves, so off-site-only filtering
# throws away exactly what is being looked for.

all:https://keskkonnaportaal.ee/sites/default/files/andmekataloog/API_liides/
all:https://www.ilmateenistus.ee/teenused/ilmainfo/eesti-prognoos-xml/
all:https://www.ilmateenistus.ee/teenused/ilmainfo/eesti-vaatlusandmed-xml/
all:https://keskkonnaportaal.ee/et/avaandmed/keskkonna-ja-ilma-valdkonna-andmeteenused
